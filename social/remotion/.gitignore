node_modules/
out/
.remotion/
*.log
# Fonts are copied in from assets/fonts/ at build time (see README), not committed.
public/fonts/*.ttf
# Pexels b-roll intro clips are downloaded at build time, never committed.
public/intro/
